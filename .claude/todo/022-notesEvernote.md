Add to project/board page view somewhere multilingual button "Notes" and then open a new Evernote like view where user can see, add, edit and delete rich text editable (see how card is rich text editable and re-use that rich tect editor) content.

It is a bigger task so break it into smaller tasks and create todo plan first. Then mplement the first task.